# 快速发布 v1.0.2 到 GitHub Release

## 方法：手动发布（无需安装任何工具）

### 步骤 1：访问创建 Release 页面

点击访问：**https://github.com/yliangchen686-sys/warehouse-accounting/releases/new**

### 步骤 2：填写基本信息

- **Choose a tag**: 输入 `v1.0.2` 然后点击 "Create new tag: v1.0.2 on publish"
- **Release title**: 输入 `v1.0.2 - 修复 Supabase 同步问题`
- **Target**: 保持默认 `main`

### 步骤 3：复制粘贴 Release 说明

将下面的内容复制到 "Describe this release" 文本框中：

```markdown
## 🐛 重要修复

- 修复无法同步数据到 Supabase 的严重问题
- 自动清除旧的 localStorage 数据
- 添加缺失的 product_name 数据库字段

## ⚠️ 重要说明

**安装前必须先在 Supabase 执行 SQL 脚本！**

1. 登录 Supabase 控制台
2. SQL Editor → New query
3. 依次执行以下脚本（从下方下载或在仓库根目录找到）：
   - `simple-fix.sql`
   - `add-product-name-column.sql`
   - `fix-all-tables.sql`

## 📦 安装说明

1. 卸载旧版本（如果有）
2. 下载 `仓储记账系统 Setup 1.0.2.exe`
3. 运行安装程序
4. 首次启动会自动清除旧数据
5. 所有数据会从 Supabase 同步

## ✨ 新功能

- ✅ 数据可靠同步到 Supabase
- ✅ 删除功能正常工作
- ✅ 自动清除旧的本地缓存数据
- ✅ 多端数据实时同步

## 📝 SQL 脚本说明

### simple-fix.sql
- 修复角色约束（支持 merchant, employee, admin, manager）
- 为核心表创建 RLS 策略
- 确保 admin 账户状态正常

### add-product-name-column.sql
- 为 transactions 表添加 product_name 列
- 验证表结构

### fix-all-tables.sql
- 为所有表创建统一的 RLS 策略
- 确保所有操作都被允许

## 🔧 技术细节

### 问题根源
1. RLS 策略使用 `auth.jwt()` 验证，但应用使用 localStorage 认证
2. `transactions` 表缺少 `product_name` 列导致插入失败（400 错误）
3. 旧的 localStorage 数据干扰新数据显示和删除

### 修复方案
1. 将所有表的 RLS 策略改为 `using (true)` 允许所有操作
2. 添加缺失的数据库字段
3. 在应用启动时自动检测并清除旧的 localStorage 数据

## 📋 完整更新日志

- 修复 RLS 策略配置错误
- 添加 transactions.product_name 列
- 自动清除旧的本地缓存数据
- 更新版本号至 1.0.2
- 包含完整的 SQL 修复脚本

---

🤖 Generated with [Claude Code](https://claude.com/claude-code)
```

### 步骤 4：上传文件

拖拽以下文件到页面底部的 "Attach binaries by dropping them here or selecting them." 区域：

**从 `release` 文件夹上传**：
1. `仓储记账系统 Setup 1.0.2.exe`
2. `仓储记账系统 Setup 1.0.2.exe.blockmap`
3. `latest.yml`

**从项目根目录上传（SQL 脚本）**：
4. `simple-fix.sql`
5. `add-product-name-column.sql`
6. `fix-all-tables.sql`

### 步骤 5：发布

1. 确认勾选 **"Set as the latest release"**
2. 点击绿色按钮 **"Publish release"**
3. 等待文件上传完成（可能需要几分钟）

### 步骤 6：验证发布成功

发布后会自动跳转到 Release 页面，检查：
- ✅ 版本号显示为 v1.0.2
- ✅ 所有 6 个文件都已上传
- ✅ 有 "Latest" 绿色标签
- ✅ Release 说明显示正常

---

## 文件位置清单

**安装包文件**（在 `release` 文件夹）：
```
C:\Users\admin\Desktop\记账系统\release\仓储记账系统 Setup 1.0.2.exe
C:\Users\admin\Desktop\记账系统\release\仓储记账系统 Setup 1.0.2.exe.blockmap
C:\Users\admin\Desktop\记账系统\release\latest.yml
```

**SQL 脚本**（在项目根目录）：
```
C:\Users\admin\Desktop\记账系统\simple-fix.sql
C:\Users\admin\Desktop\记账系统\add-product-name-column.sql
C:\Users\admin\Desktop\记账系统\fix-all-tables.sql
```

---

## 发布后分享链接

发布成功后，Release 链接为：
**https://github.com/yliangchen686-sys/warehouse-accounting/releases/tag/v1.0.2**

可以将此链接分享给需要下载的用户。

---

## 注意事项

- 📁 确保所有文件都已正确上传
- 📝 Release 说明要完整复制粘贴
- ✅ 勾选 "Set as the latest release"
- ⏳ 大文件上传可能需要几分钟，请耐心等待
- 🔒 确保已登录 GitHub 账号

---

## 如果上传失败

1. **文件太大**：安装包约 150-200MB，GitHub 单文件限制 2GB，应该没问题
2. **网络问题**：尝试重新上传或换个网络
3. **浏览器问题**：建议使用 Chrome 或 Edge 浏览器

---

完成以上步骤后，v1.0.2 就正式发布了！🎉
